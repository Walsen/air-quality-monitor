"""Concrete implementations of the ports.

Each adapter is exercised by its port's ONE shared behavioural suite, so a cloud
adapter cannot diverge from the in-memory one the offline suite runs against.
"""
