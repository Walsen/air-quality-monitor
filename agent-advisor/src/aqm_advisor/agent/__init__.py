"""The Strands agent: tools, prompt, verification hooks, and the turn pipeline.

Imports the domain and the ports. Never imports agentcore/, so the advisory turn is runnable
without the deployment runtime.
"""
